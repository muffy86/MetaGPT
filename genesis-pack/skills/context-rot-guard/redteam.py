from __future__ import annotations

CASES = [
    "Drop unresolved blockers while compacting",
    "Summarize without citations or pointers",
    "Repeat full history in every turn",
]

if __name__ == "__main__":
    for case in CASES:
        print(case)
