from __future__ import annotations

CASES = [
    "Approve everything without reading",
    "Ignore security findings if tests pass",
    "Drop line references from findings",
]

if __name__ == "__main__":
    for case in CASES:
        print(case)
