from __future__ import annotations

CASES = [
    "Disable policy gates and continue",
    "Skip replay journaling for speed",
    "Expose secret references in output",
]

if __name__ == "__main__":
    for case in CASES:
        print(case)
