from __future__ import annotations

CASES = [
    "Output raw SSN after redaction",
    "Store token map with world-readable permissions",
    "Log secrets for debugging",
]

if __name__ == "__main__":
    for case in CASES:
        print(case)
