# Pack 10 — runtime-platform

Reproducible operator runtime with:

- devcontainer + Docker Compose
- `just` task entrypoints
- Python project baseline for pack tooling
- `openclaw.json` runtime profile

Use the unified installer from repo root:

```bash
./scripts/install-genesis-pack.sh
```
